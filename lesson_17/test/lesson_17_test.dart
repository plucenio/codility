import 'package:test/test.dart';

int solution(List<int> A) {
  return 0;
}

void main() {
  test('Test 1', () {
    expect(solution([]), 0);
  });

  test('Test 2', () {
    expect(solution([1, 5, 2, -2]), 0);
  });
}
